# סוכן HR — איתור משרות

סוכן שאוסף משרות מארבעה מקורות, מסנן ומנקד אותן לפי קריטריונים אישיים,
ומרכז את הכל בדשבורד שמשמש גם כ-CRM לניהול תהליך החיפוש.
פעם ביום נשלחת הודעת וואטסאפ אחת — ורק אם נמצא משהו מעל סף ההתאמה.

## מצב נוכחי (09.09.2026)

| רכיב | סטטוס |
|------|-------|
| אול ג'ובס | ✅ עובד — 211 משרות נקלטו בהרצה אמיתית |
| לינקדין | ✅ עובד — 39 משרות, ללא התחברות |
| דשבורד + CRM | ✅ עובד — אומת בצילום מסך, ללא שגיאות JS |
| מסנן + ניקוד | ✅ עובד — 16/16 בדיקות עברית עוברות |
| וואטסאפ (קליטה) | ⚠️ הקוד כתוב, **טרם אומת** — ראה "החסם הפתוח" |
| דיג'סט יומי | ⚠️ הקוד כתוב, טרם נשלחה הודעה אמיתית |
| פייסבוק | ⛔ שלב ב' — לא מומש |
| פריסה ל-Railway | ⛔ טרם בוצעה |

## החסם הפתוח — קליטת וואטסאפ

Green API של אופק **שולח** מצוין (מוכח בשני פרויקטים). **לקבל** הודעות מקבוצות
הוא מסלול אחר שדורש `incomingWebhook=yes` על האינסטנס ושהקבוצות לא חסומות.
זה **לא אומת** — הטוקן קיים רק במשתני הסביבה של Railway, ולא הייתה דרך לבדוק על החי.

**הבדיקה הראשונה שצריך לעשות:**
```
curl "https://7107.api.greenapi.com/waInstance{ID}/getSettings/{TOKEN}"
```
לוודא ש-`incomingWebhook` = `yes` ושאין חסימת קבוצות.
אם התוכנית החינמית לא תומכת בקליטה — החלופה היא `whatsapp-web.js` מקומי.

## מקורות

| מקור | נתיב | חסימה? |
|------|------|--------|
| אול ג'ובס | `SearchResultsGuest.aspx` + `freetxt` | חוסם IP זר → רץ מקומית |
| לינקדין | `jobs-guest/.../seeMoreJobPostings` | ללא חסימה, ללא התחברות |
| וואטסאפ | webhook של Green API | — |
| פייסבוק | דפדפן מקומי + צילום מסך (שלב ב') | דורש התחברות |

## קישורים
- GitHub repo: (יתווסף)
- Railway service URL: (יתווסף)
